# Decision Lab 제품 요구사항 문서

## 1. 문서 개요

### 1.1 제품명

Decision Lab

### 1.2 제품 한 줄 설명

Decision Lab은 복잡한 고민을 선택지, 판단 기준, 중요도로 구조화하고 사용자가 스스로 납득할 수 있는 결정을 내리도록 돕는 AI 의사결정 지원 웹앱이다.

### 1.3 문서 목적

이 문서는 Decision Lab의 첫 번째 제품 버전(MVP)을 개발하기 위한 제품 방향, 사용자 경험, 기능 요구사항, 계산 규칙 및 성공 기준을 정의한다.

### 1.4 핵심 원칙

- AI가 사용자를 대신해 정답을 결정하지 않는다.
- AI 기능은 Microsoft Azure 구독의 Microsoft Foundry Model Router를 통해 제공한다.
- 사용자는 내부적으로 선택된 개별 모델보다 일관된 Decision Lab 경험과 결과 근거를 확인한다.
- 사용자의 우선순위와 판단 근거를 명시적으로 드러낸다.
- 중요도나 조건을 바꿨을 때 결과가 어떻게 달라지는지 보여준다.
- 결론보다 결론에 이르는 과정을 설명한다.
- 불확실하거나 근거가 부족한 정보는 확정된 사실처럼 표현하지 않는다.
- 사용자가 충분히 비교한 뒤 결정을 확정하고 기록할 수 있도록 돕는다.

## 2. 문제 정의

사용자는 구매, 진로, 여행, 학업, 인간관계 등 다양한 상황에서 여러 선택지를 비교해야 한다. 하지만 정보가 많아질수록 비교 기준이 뒤섞이고, 자신에게 무엇이 중요한지 정하지 못하며, 잘못된 선택에 대한 불안으로 결정을 미루게 된다.

기존 검색이나 추천 서비스는 정보를 더 제공하거나 하나의 답을 제시하는 데 집중한다. Decision Lab은 사용자의 상황을 구조화하고, 판단 기준과 중요도를 함께 조정하며, 선택 결과의 안정성과 한계를 보여주는 데 집중한다.

## 3. 목표와 비목표

### 3.1 제품 목표

1. 사용자가 자신의 고민을 짧은 자연어로 입력할 수 있게 한다.
2. AI가 고민의 핵심, 선택지, 판단 기준을 구조화하도록 한다.
3. 사용자가 판단 기준의 중요도를 직접 수정할 수 있게 한다.
4. 중요도를 반영한 선택지별 종합 점수와 비교 근거를 제공한다.
5. What-if 실험으로 어떤 조건에서 결과가 바뀌는지 보여준다.
6. 결정 안정성을 설명하고 최종 결정을 기록할 수 있게 한다.

### 3.2 비목표(MVP 제외)

- 전문적인 의료, 법률, 투자 또는 안전 판단을 대신하는 기능
- 실시간 가격, 재고, 최신 뉴스 등 외부 데이터의 자동 수집 및 보증
- 여러 사용자가 함께 편집하는 협업 기능
- 사용자의 과거 기록을 이용한 개인화 모델 학습
- 완전 자동화된 구매, 예약 또는 외부 서비스 실행
- 사용자가 요청마다 특정 기반 모델을 직접 선택하는 기능

## 4. 대상 사용자

### 4.1 주요 사용자

선택지가 많거나 판단 기준이 복잡해 결정을 미루고, 다른 사람의 단순 추천보다 자신의 상황에 맞는 비교 과정을 원하는 사람.

### 4.2 대표 사용자 시나리오

#### 시나리오 A: 노트북 구매

사용자는 게임과 사무 작업에 모두 적합한 노트북을 고르지 못하고 있다. 예산, 게임 성능, 업무 성능, 휴대성, 배터리를 기준으로 비교하고 싶다. 사용자는 각 기준의 중요도를 조절한 뒤 결과가 바뀌는 조건을 확인하고 구매 결정을 기록한다.

#### 시나리오 B: 진로 선택

사용자는 두 가지 진로 중 무엇을 선택할지 고민한다. 연봉, 성장 가능성, 안정성, 흥미, 생활 방식과 같은 기준을 직접 확인하고, 점수만이 아니라 각 선택지의 장단점과 자신의 우선순위를 함께 검토한다.

## 5. 제품 경험 및 사용자 흐름

```text
고민 입력
  -> AI 분석 결과 확인
  -> 선택지 확인 및 추가/삭제
  -> 판단 기준 확인 및 추가/삭제
  -> 기준별 중요도 설정
  -> 선택지별 평가 및 근거 확인
  -> 종합 결과와 결정 안정성 확인
  -> What-if 실험
  -> AI 결정 분석 확인
  -> 결정 확정 및 기록
```

### 5.1 시작

사용자는 고민 내용을 자유롭게 입력한다. 선택지를 이미 알고 있다면 함께 입력할 수 있고, 모른다면 AI가 초안으로 제안한다.

### 5.2 분석 확인

AI는 입력 내용을 다음 항목으로 요약한다.

- 고민의 핵심 질문
- 사용자가 원하는 결과
- 현재 확인된 선택지
- 추천 판단 기준
- 추가로 확인하면 좋은 질문

사용자는 AI가 분석한 내용을 수정하거나 승인한다. AI의 제안을 승인하지 않아도 다음 단계로 진행할 수 있어야 한다.

### 5.3 선택지와 판단 기준 정리

사용자는 선택지를 추가, 수정, 삭제할 수 있다. 판단 기준 역시 AI 추천 항목을 수정하거나 직접 추가할 수 있다. MVP에서는 비교 대상이 지나치게 많아지지 않도록 선택지와 기준의 권장 개수를 안내한다.

### 5.4 중요도 설정

각 판단 기준에 1~5의 중요도를 설정한다. 중요도 변경 시 결과 점수와 순위가 즉시 다시 계산되어야 한다.

### 5.5 비교 및 결과 확인

각 선택지는 기준별 점수와 평가 근거를 가진다. 결과 화면은 종합 점수, 기준별 기여도, 강점, 약점, 점수 차이를 함께 보여준다.

### 5.6 What-if 실험

사용자가 하나 이상의 기준 중요도를 조절하면 결과를 임시 상태로 계산한다. 저장된 기본 결과와 실험 결과를 구분하고, 순위가 바뀌는 경우 어떤 기준의 변화가 영향을 주었는지 설명한다.

### 5.7 결정 확정

사용자는 결과를 검토한 후 선택지를 결정으로 확정한다. 확정 전에는 선택지를 다시 비교할 수 있고, 확정 후에도 기록을 열람하거나 실제 사용 후기를 추가할 수 있어야 한다.

## 6. 기능 요구사항

### FR-01. 고민 입력

- 사용자는 제목과 고민 내용을 입력할 수 있어야 한다.
- 입력 내용은 빈 값일 수 없다.
- 사용자는 선택적으로 선택지, 예산, 기한, 제외 조건을 함께 입력할 수 있다.
- 분석 중에는 진행 상태를 표시하고 중복 요청을 방지해야 한다.

### FR-02. AI 고민 분석

- AI는 고민 내용을 핵심 질문과 요약으로 변환해야 한다.
- AI는 판단 기준을 이름, 설명, 추천 이유와 함께 제안해야 한다.
- AI는 입력에서 직접 확인되지 않은 사실을 사용자 사실로 단정해서는 안 된다.
- 정보가 부족한 경우 최소한의 추가 질문을 제안해야 한다.
- 사용자는 분석 결과의 각 항목을 승인, 수정, 삭제할 수 있어야 한다.
- 분석 요청은 Microsoft Foundry Model Router의 논리적 배포를 통해 처리해야 한다.
- 라우터가 선택한 기반 모델명은 제품 기능 계약의 일부가 아니며, 결과 화면에 필요 이상으로 노출하지 않는다.

### FR-03. 선택지 관리

- 사용자는 선택지를 추가, 수정, 삭제할 수 있어야 한다.
- 선택지에는 이름, 설명, 장점, 단점, 메모를 입력할 수 있어야 한다.
- AI가 제안한 선택지와 사용자가 직접 입력한 선택지를 구분해야 한다.
- 선택지가 2개 미만이면 비교 결과를 생성할 수 없어야 한다.

### FR-04. 판단 기준 관리

- 사용자는 판단 기준을 추가, 수정, 삭제할 수 있어야 한다.
- 판단 기준에는 이름, 설명, 중요도, 평가 방향(높을수록 좋음/낮을수록 좋음)을 저장해야 한다.
- 모든 기준을 삭제할 수 없도록 최소 1개 기준을 유지해야 한다.
- 중요도는 1~5 범위에서 설정한다.

### FR-05. 선택지 평가 및 점수화

- 각 선택지는 각 판단 기준에 대해 0~100점의 점수를 가져야 한다.
- 각 점수에는 짧은 평가 근거와 정보 출처 유형(사용자 입력, AI 추정, 사용자 직접 평가)을 표시해야 한다.
- 사용자는 AI 평가를 수정할 수 있어야 한다.
- 종합 점수는 중요도를 반영한 가중 평균으로 계산해야 한다.

기준 $i$의 중요도를 $w_i$, 선택지 $j$의 기준별 점수를 $s_{ij}$라고 할 때 종합 점수 $S_j$는 다음과 같이 계산한다.

$$
S_j = \frac{\sum_{i=1}^{n} w_i s_{ij}}{\sum_{i=1}^{n} w_i}
$$

- 중요도는 점수의 가중치로만 사용하며, 원점수 자체를 임의로 변경하지 않는다.
- 점수가 같거나 데이터가 부족한 경우 순위를 억지로 구분하지 않는다.

### FR-06. 결과 시각화

- 선택지별 종합 점수를 비교할 수 있는 막대그래프를 제공해야 한다.
- 기준별 강점과 약점을 표 또는 상세 패널로 제공해야 한다.
- 레이더 차트는 보조 시각화로 제공할 수 있으며, 차트만으로 의미를 전달해서는 안 된다.
- 모든 시각 정보는 텍스트와 숫자로도 확인할 수 있어야 한다.

### FR-07. What-if 실험

- 사용자는 기준별 중요도를 임시로 변경할 수 있어야 한다.
- 실험 중 결과를 기본 결과와 구분해야 한다.
- 종합 점수, 순위, 1위와 2위의 점수 차이를 즉시 갱신해야 한다.
- 순위가 바뀌면 순위 역전이 발생한 기준과 변경 내용을 설명해야 한다.
- 사용자는 실험 결과를 기본 설정으로 적용하거나 취소할 수 있어야 한다.

### FR-08. 결정 안정성 분석

- 1위와 2위의 점수 차이를 계산해야 한다.
- 안정성은 점수 차이만으로 결정되는 참고 지표이며 절대적인 확률이나 보증으로 표현하지 않는다.
- 결과는 예를 들어 `높음`, `보통`, `낮음`과 같이 이해하기 쉬운 상태로 표시한다.
- 안정성이 낮으면 추가로 확인할 기준이나 직접 검토할 질문을 제안해야 한다.

MVP의 초기 안정성 구간은 전체 점수 범위 0~100을 기준으로 다음과 같이 정의한다. 실제 사용 데이터가 쌓이면 조정한다.

- 10점 이상: 높음
- 5점 이상 10점 미만: 보통
- 5점 미만: 낮음

### FR-09. AI 결정 분석

- AI는 1위 선택지가 높은 점수를 받은 주요 기준을 설명해야 한다.
- AI는 해당 선택지의 약점과 상충하는 기준을 함께 제시해야 한다.
- 점수와 중요도에 근거하지 않은 확정적 표현을 피해야 한다.
- 사용자가 최종 판단을 내리도록 남은 확인 질문을 제공해야 한다.

### FR-10. 결정 확정 및 기록

- 사용자는 선택지 하나를 최종 결정으로 확정할 수 있어야 한다.
- 기록에는 고민 내용, 선택지, 판단 기준, 중요도, 기준별 점수, 종합 점수, 분석 시점, 최종 선택을 저장해야 한다.
- 사용자는 결정 기록을 다시 볼 수 있어야 한다.
- 사용자는 결정 후 실제 만족도, 실제로 중요했던 기준, 회고 메모를 추가할 수 있어야 한다.
- 확정된 결정은 삭제하거나 재검토할 수 있어야 한다.

### FR-11. Microsoft Foundry Model Router 연동

- 모든 MVP AI 작업(`analyzeProblem`, `suggestOptions`, `suggestCriteria`, `evaluateOptions`, `explainDecision`)은 Foundry Model Router를 사용하는 서버 측 AI 어댑터를 통해 호출해야 한다.
- 애플리케이션은 개별 기반 모델의 이름이나 버전을 코드에 하드코딩하지 않고 Foundry의 Router 배포 이름을 논리적 모델 식별자로 사용해야 한다.
- 라우터가 선택한 모델, 응답 지연, 토큰 사용량, 요청 성공 여부는 운영 관찰성을 위해 기록할 수 있어야 한다. 단, 고민 원문과 민감 정보는 로그에 기록하지 않는다.
- 라우터 정책 또는 기반 모델 구성이 변경되어도 서버의 구조화 응답 스키마와 점수 계산 규칙은 변하지 않아야 한다.
- Foundry 호출 실패, 응답 스키마 오류, 사용량 제한 시 사용자가 수동으로 선택지와 기준을 입력해 계속 진행할 수 있어야 한다.

## 7. 화면 요구사항

### 7.1 홈/새 결정 화면

- 서비스 소개를 짧게 제공한다.
- 고민 입력 폼을 가장 눈에 띄는 영역에 배치한다.
- 예시 고민을 선택해 입력할 수 있다.
- 새 결정 시작 버튼을 제공한다.

### 7.2 분석 결과 화면

- AI 요약과 추천 기준을 보여준다.
- 선택지와 판단 기준을 편집할 수 있다.
- 추가 질문이 있다면 사용자가 답한 뒤 분석을 갱신할 수 있다.

### 7.3 비교 설정 화면

- 기준별 중요도 조절 UI를 제공한다.
- 선택지별 기준 점수와 평가 근거를 편집할 수 있다.
- 변경 사항이 결과에 미치는 영향을 미리 확인할 수 있다.

### 7.4 결과 화면

- 최종 순위와 종합 점수를 우선 표시한다.
- 점수 계산에 기여한 기준을 표시한다.
- What-if 실험 패널을 제공한다.
- 결정 안정성 및 AI 분석을 제공한다.
- 결정 확정 버튼과 재검토 버튼을 제공한다.

### 7.5 결정 기록 화면

- 과거 결정 목록을 최신순으로 제공한다.
- 기록 상세에서 당시 설정과 결과를 재현할 수 있어야 한다.
- 실제 사용 후 회고를 추가할 수 있어야 한다.

## 8. 데이터 모델 초안

### Decision

- `id`
- `title`
- `problem_statement`
- `summary`
- `status`: `draft`, `analyzing`, `comparing`, `decided`, `archived`
- `created_at`
- `updated_at`
- `decided_at`
- `selected_option_id`

### Option

- `id`
- `decision_id`
- `name`
- `description`
- `pros`
- `cons`
- `source`: `user`, `ai`
- `created_at`

### Criterion

- `id`
- `decision_id`
- `name`
- `description`
- `importance`: 1~5
- `direction`: `higher_is_better`, `lower_is_better`
- `source`: `user`, `ai`

### Evaluation

- `id`
- `option_id`
- `criterion_id`
- `score`: 0~100
- `reason`
- `source`: `user`, `ai`
- `updated_at`

### DecisionSnapshot

- `id`
- `decision_id`
- `criteria_weights`
- `option_scores`
- `total_scores`
- `stability_level`
- `created_at`

### Retrospective

- `decision_id`
- `satisfaction_score`
- `important_in_practice`
- `memo`
- `created_at`

## 9. AI 요구사항 및 안전 기준

- AI 응답은 구조화된 JSON 계약으로 반환해 UI가 안정적으로 처리할 수 있어야 한다.
- 분석, 선택지 제안, 기준 제안, 평가, 결정 분석을 별도 단계로 분리한다.
- AI가 입력에 없는 가격, 성능, 통계, 사실을 만들어내지 않도록 한다.
- 외부 정보가 필요하면 정보의 최신성 및 출처 확인이 필요하다는 안내를 표시한다.
- 의료, 법률, 투자, 안전 관련 고민에는 참고용 도구임을 명확히 하고 전문가 상담을 권고한다.
- 사용자의 민감한 정보는 최소한으로 수집하고, 모델 호출과 저장 시 보호한다.
- AI 결과를 사용자가 수정하면 사용자 입력을 최종 기준으로 취급한다.

## 10. 비기능 요구사항

### 성능

- 사용자가 입력한 변경 사항에 대한 점수 재계산은 일반적으로 300ms 이내에 완료한다.
- AI 분석 요청은 진행 상태, 성공, 실패, 재시도 상태를 명확히 표시한다.
- 결과 화면의 핵심 데이터는 AI 응답이 지연되어도 먼저 렌더링할 수 있어야 한다.
- Foundry Model Router 호출의 타임아웃, 재시도, rate limit 상태를 사용자에게 이해 가능한 진행/오류 상태로 표시한다.

### 접근성 및 반응형

- 키보드만으로 모든 핵심 흐름을 완료할 수 있어야 한다.
- 색상만으로 점수나 상태를 구분하지 않는다.
- 모바일 화면에서도 기준 조절, 결과 확인, 결정 확정이 가능해야 한다.
- 차트는 대체 텍스트와 표 형태의 데이터를 함께 제공한다.

### 신뢰성

- 점수 계산은 동일한 입력에 대해 항상 동일한 결과를 반환해야 한다.
- 사용자가 변경한 기준과 점수는 AI 재분석으로 덮어쓰지 않는다.
- AI 호출 실패 시 사용자가 직접 선택지와 기준을 입력해 진행할 수 있어야 한다.
- 저장되지 않은 What-if 실험 결과와 확정된 결정 결과를 혼동하지 않게 한다.

## 11. 성공 지표

### 핵심 지표

- 결정 완료율: 결정을 시작한 사용자 중 최종 결정을 확정한 비율
- 첫 결정까지 걸린 시간
- 결정당 What-if 실험 횟수
- 결정 기록 재방문율
- 결정 후 회고 작성률

### 질적 지표

- 사용자가 자신의 최종 판단 기준을 명확히 설명할 수 있는지
- 사용자가 AI 추천을 그대로 따르지 않고 기준과 근거를 검토했는지
- 결정 후 불필요한 재비교가 줄었는지
- 결과가 사용자의 실제 만족도와 어떤 관계가 있는지

## 12. MVP 범위 및 우선순위

### P0: 첫 사용 가능한 제품

1. 고민 입력
2. AI 고민 요약 및 판단 기준 추천
3. 선택지/판단 기준 수동 편집
4. 중요도 1~5 설정
5. 기준별 점수와 종합 점수 계산
6. 결과 화면과 결정 안정성 표시
7. 결정 확정 및 로컬 또는 서버 저장

### P1: 핵심 차별화 기능

1. What-if 실험
2. AI 결정 분석
3. 점수 근거 편집
4. 결정 기록 상세 및 회고
5. 결과 비교 차트

### P2: 확장 기능

1. 과거 결정 기반 개인화
2. 결정 패턴 분석
3. 외부 데이터 출처 연동
4. 공유 및 협업
5. 도메인별 템플릿

## 13. 출시 전 검증 시나리오

1. 사용자가 노트북 구매 고민을 입력하고 AI 분석 결과를 수정할 수 있다.
2. 선택지 2개 이상과 판단 기준 1개 이상으로 비교를 완료할 수 있다.
3. 중요도 변경 시 종합 점수와 순위가 즉시 바뀐다.
4. 점수 차이에 따라 안정성 상태가 올바르게 표시된다.
5. AI 분석 실패 후에도 수동 입력으로 결정을 완료할 수 있다.
6. What-if 결과를 저장하지 않고 취소할 수 있다.
7. 결정을 확정한 뒤 당시의 기준, 중요도, 점수를 다시 볼 수 있다.
8. 모바일과 키보드 환경에서 핵심 흐름을 완료할 수 있다.

## 14. 미결정 사항

- 로그인과 익명 사용 중 어떤 방식으로 MVP를 시작할지
- Foundry Model Router에서 사용할 프로젝트, 리전, Router 배포명과 모델 라우팅 정책
- Foundry Model Router 호출 비용 및 사용자/세션별 사용량 제한
- 외부 데이터 검색 및 출처 표시 범위
- 선택지별 점수를 AI가 자동 산정할지 사용자가 직접 산정할지
- 안정성 구간을 고정값으로 둘지 과거 데이터 기반으로 조정할지
- 결정 기록의 보관 기간과 삭제 정책
- 민감한 고민 입력에 대한 저장 및 비식별화 정책